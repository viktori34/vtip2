def func (s, n):
    """Сравнивает длину строки 
    с введеным значением, если 
    длина строки больше, то возвращает строку в верхнем регистре,
    иначе возвращает строку"""
    if len(s)> n:
        return s.upper()
    else:
        return s
